# Namespace는 Kubernetes object를 논리적으로 묶는 공간이다.
# 이번 챕터의 리소스를 default namespace에 흩뿌리지 않고,
# 실습이 끝나면 namespace 단위로 정리할 수 있게 분리한다.
apiVersion: v1
kind: Namespace
metadata:
  # 이후 Deployment, Service, PVC, Ingress가 모두 이 namespace 안에 만들어진다.
  name: model-serving
