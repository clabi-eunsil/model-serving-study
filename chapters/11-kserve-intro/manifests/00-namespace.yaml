# KServe InferenceService를 배포할 실습 namespace다.
#
# KServe 공식 튜토리얼도 control plane namespace가 아닌 별도 namespace에
# InferenceService를 배포하도록 안내한다.
# control plane namespace에 배포하면 webhook/storage initializer 동작이 달라져
# 모델 파일을 찾지 못하는 문제가 생길 수 있다.
apiVersion: v1
kind: Namespace
metadata:
  name: kserve-test
